name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: build
      run: mkdir ./cmake-build-debug && cmake --build ./cmake-build-debug --target cache -- -j 6
